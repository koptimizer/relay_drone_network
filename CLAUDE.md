# 프로젝트 개요
본 프로젝트는 재난 상황에서의 긴급 물자 배송 라우팅 최적화를 위해 중계 드론 네트워크(Relay Drone Network)을 이용한 다중 드론 제어를 강화학습으로 접근하는 연구를 다룬다. 강화학습이 가진 빠른 추론 속도를 강점으로 하는 모델을 연구/개발하는 것을 목표로 한다. 기존의 연구 사항은 docs/recent_study_before_260824.pdf에 자세히 담겨있다.

## 1. 문제 상황
* **통신 및 인프라 부재:** 재난 상황에서는 기존의 통신/물류/교통 인프라가 파괴되어 통신 반경이 제한되거나, 장애물로 인한 직접 통신이 불가능한 사각지대가 발생한다.
* **긴급 물자 수송의 특수성:** 긴금 물자는 배송 완료 중요도가 높고 상/하역 시 세밀한 제어가 필요하므로, 영상 데이터 실시간 전송 및 배송 상황 추적을 위한 100% 통신 연결(n-hop 중계 포함)이 필수적이다.
* **릴레이 드론 네트워크의 필요성:** 재난 외부 지역에서 임시 통신 시설을 구축하고 각 드론이 통신을 중계하며 드론의 임무 수행 반경을 넓히는 릴레이 드론 네트워크를 이용하여 다중 드론을 제어함으로써 광범위한 긴급 물자 수송이 가능하다.


## 2. 주요 가정

* 모든 드론은 중앙 제어 센터에서 지정 capacity만큼 물자를 적재하여 출발한다.
* 모든 드론은 날씨 등 외란에 의한 속도 변화는 고려하지 않고, 기체 간 서로 충돌하지 않는다.
* 모든 드론은 물자를 목적지로 배송할 수 있고, 통신 중계를 수행할 수 있다.
* 모든 목적지에 물자를 전달해야하며, 중앙 제어 센터에서 물자를 추가 적재할 수 있다.
* 물자 하역 / 물자 재적재를 위해 목적지 / 중앙 제어 센터에 도착하면 일정 시간동안 하역 / 재적재 수행 시간이 소요된다.
* 모든 드론은 임무 수행 간 통신 활성화 상태여야 한다.
* 모든 목적지에 정해진 물자 양이 모두 만족되는 (배송이 완료되는) 최종 배송 시간을 최소화하는 것을 목적으로 한다.
* 배터리 소모 및 충전은 고려하지 않는다.

## 3. 제안 알고리즘 방향성
기존 MILP(혼합 정수 계획법) 모델은 실시간 n-hop 통신 유지 여부를 계산할 때 복잡도가 기하급수적으로 폭발하는 한계가 있다. 이를 해결하기 위해 강화학습(RL) 기반의 접근을 제안한다.

* **MARL 기반 프레임워크:** Centralized Training with Decentralized Execution (CTDE) 구조를 채택한 Multi-Agent Reinforcement Learning을 적용한다.
* **알고리즘:** 제어 문제에서 높은 성능을 보이는 Twin-Q 기반의 Soft Actor-Critic (SAC) 알고리즘을 사용한다.
* **고도화 제안 (Graph Neural Networks 활용):** 드론 간의 통신 중계 네트워크와 배송지 간의 복잡한 구조적 의존성을 효과적으로 모델링하기 위해, 에이전트의 관측(Observation) 공간을 인코딩할 때 그래프 신경망(GNN) 기반의 공간-시간적(spatio-temporal) 그래프 아키텍처를 도입하여 협력 체계를 고도화하는 방향을 탐구한다.
* **상태 및 보상 설계:**
    * **State (Observation):** 통신 연결 여부, 현재 적재량, 목표 수행 상태(배송중/귀환중), 임시 목표까지의 거리 및 방향 벡터, 인접 드론의 상태 등.
    * **Reward Function:** $R = R_{shape} + R_{time} + R_{collision} + R_{relay} + team_{reward}$
        * 거리에 따른 보상($R_{shape}$), 지연 페널티($R_{time}$), 충돌 페널티/산개 보상($R_{collision}$), 통신 단절 페널티($R_{relay}$), 배달 성공 및 재적재에 따른 대량의 팀 보상($team_{reward}$).
* **극복 과제:**
    * **학습 불균형 극복:** 5,000 에피소드까지의 초기 학습 시 일부 드론이 임무를 수행하지 못하거나 원거리 배송을 완료하지 못하는 현상이 관찰되었다.
    * **보상 함수 튜닝:** 의도된 드론 기동(relay 유지 및 신속한 배송)을 유도할 수 있도록 각 페널티와 보상 가중치를 세밀하게 재조정할 필요가 있다.
    * **Hyperparameter 최적화:** SAC 알고리즘의 엔트로피 가중치(Alpha) 및 GNN 인코더의 학습률 등 하이퍼 파라미터 튜닝을 통해 안정적인 수렴을 확보할 필요가 있다.


## 4. 프로젝트 구조 (File Tree)
DRONE
├── docs
│     ├── tex: 구현 사항, 실험 결과, 결과 insight 줄 글 정리 (영논문 형식)
│     └──  pptx: 학회 또는 연구 미팅 자료 생성
├── .claude: agent 및 setting 용 json 파일
│   └── agents/
│       ├── researcher.md
│       ├── reviewer.md
│       └── verifier.md
├── figures: 시각화 결과 저장 파일
├── legacy: 사용되지는 않지만, 재사용성을 위해 저장해둔 legacy 코드 모음들
│   ├── milp/: Gurobi 수리모형(model.py, *.lp, 인스턴스 json) 및 전용 시각화
│   └── pre_v1/: v1 분할 이전 원본(CTDERL.py, CTDERL_test.py, instance_generator.py)
├── proposed_src: 제안 강화학습 프레임워크의 환경, 에이전트 네트워크, 학습 및 추론, 유틸 코드
│   ├── env/: 재난 릴레이 드론 환경
│   ├── model/: SAC actor / twin-Q critic 네트워크
│   ├── pipeline/: 리플레이 버퍼, 학습 루프, 평가 루프
│   └── util/: 인스턴스 생성기, 지도 데이터, 로깅 및 시각화
├── ref: 참고 레퍼런스 조사
│   ├── details.md
│   ├── SKILL.md
├── runs: TensorBoard 학습 로그 (버전별 하위 폴더)
├── weights: 학습 가중치 폴더 (버전별 하위 폴더)
└── CLAUDE.md: 본 파일

## 5. 코딩 컨벤션 합의
* 구현언어: python
* 들여쓰기: 1 tab
* 세미콜론 불가
* 주석: class = 용도 3줄, 함수 = 1줄 
* 변수명: 최대한 간결한 단어로 (ex. double C, S, M)
* Try and Exception: 쓰레딩 동기화 보호 목적 제외 사용 불가
* 깃헙 push시 "버전넘버 + YY-MM-DD-HH"형태로 태그를 달고 *10단어 이하의 간단한 설명*을 포함할 것
* 연구 관련 소스 파일은 환경(proposed_src/env), 에이전트 네트워크(proposed_src/model), 학습 루프(proposed_src/pipeline), 로깅 및 시각화 폴더(proposed_src/util)를 만들어서 각 분류하고, 접미로 나와 합의된 버전넘버 + 날짜시간(YY_MM_DD_HH)을 적을 것.
* 학습 weight나 결과 리포트 파일, docs, figure 파일들도 모두 나와 합의된 버전넘버 + 날짜시간(YY_MM_DD_HH)을 접미로 붙일 것.
* 현재 합의 버전: v4 (26_09_14_03) — 버전 차이는 docs/VERSIONS.md, 프레임워크 설명은 docs/framework_v3_26_08_31_19.md 참조

## 6. 검증 과정
* **정량적 평가 지표:** 에피소드 당 총 보상(Reward/Episode), RewardNorm Mean, Actor/Critic Loss 안정성, GradNorm 등의 지표를 실시간 트래킹한다.
* **시뮬레이션 검증:** Pygame 등을 활용한 2D/3D 시뮬레이션 환경에서 통신 가능 구역 이탈 방지, 릴레이 네트워크 형성 여부, 병목 현상 및 충돌 회피 동작이 의도된대로 작동하는지 시각적으로 검증한다.
* **비교군 설정:** 베이스라인(수리모형, 휴리스틱 등)과의 비교 실험에서 우위 이유 분석 및 Insight 도출한다.
* **자료화:** figures, docs에 한 눈에 분석되도록 업데이트한다.

## 7. 주의 사항
- 베이스라인 알고리즘을 과소 평가하거나, 내 알고리즘을 올려치기 하지 말 것

## 8. 버전 관리 (GitHub)
* 원격 저장소: https://github.com/koptimizer/relay_drone_network
* **각 버전의 연구 사이클이 완료되면 자동으로 커밋·태그·푸시할 것.** 실행 명령:
  `proposed_src/pipeline/release_cycle.sh <버전태그> "<10단어 이하 설명>"`
  (예: `release_cycle.sh v3_26_08_31_19 "hierarchical MARL matches greedy with lower variance"`)
* 사이클 완료 시점의 기준: 학습이 조기 종료로 끝나고, 홀드아웃 평가와 docs/VERSIONS.md 갱신까지 마친 때
* 저장소 용량 관리: 체크포인트는 각 실행의 `best_*.pth`만 추적한다. 에피소드별 체크포인트,
  TensorBoard 이벤트 파일, 중단된 실행의 중간 가중치는 `.gitignore`로 제외한다.
  스크립트가 50MB 초과 파일을 발견하면 푸시를 중단한다.

## 9. 실험 실행 규칙
* **다단계 실험은 연쇄 실행 스크립트로 묶을 것.** 앞 단계가 끝나면 다음 단계와 최종 평가까지
  사람 개입 없이 이어지도록 한 번에 걸어둔다. "끝나면 다음 단계를 돌리겠다"고 말만 하고
  실제로 걸지 않아 학습이 멈춘 채 며칠이 지난 사고가 두 번 있었다 (26-09-04, 26-09-07).
  단계 사이를 수동으로 잇지 말 것.
* **보고를 요청받은 실험은 종료 신호를 받는 즉시 보고할 것.** 사용자가 다시 묻기를 기다리지
  않는다. 학습 종료·평가 완료·크래시 모두 해당한다.
* 실험을 시작할 때는 종료를 감지할 수단(Monitor 또는 종료 조건 대기)을 함께 걸어둔다.
  걸지 않은 채 "완료되면 보고하겠다"고 답하지 말 것.

## 10. 응답 규칙
- 모든 답변 첫 줄에 응답 시각을 `YY-MM-DD HH:MM:SS` 형식으로 표기할 것 (예: `26-09-01 15:23:18`)
- 시각은 추정하지 말고 `date "+%y-%m-%d %H:%M:%S"`로 실제 확인한 값을 쓸 것
- 대답할때 물결표를 ("~") 잘못 붙이면 취소선이 섞여서 나오니 이를 유의해서 답변할 것. 